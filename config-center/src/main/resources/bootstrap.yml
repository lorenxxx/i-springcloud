spring:
  application:
    name: service-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lorenxxx/i-springcloud-config.git
          searchPaths: '{application}'
          label: master
          username:
          password:

server:
  port: 8060
  servlet:
    contextPath: /config-center

